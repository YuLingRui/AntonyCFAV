<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mianshi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="handler.HandlerMianShiActivity"></activity>
        <activity android:name="fragment.FragmentMSActivity"></activity>

        <service
            android:name="service.ServiceMianShi"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".MainActivity"></activity>

        <activity
            android:name="activity.ActivityMianShi"
            android:configChanges="screenLayout"></activity>

        <activity android:name="handler.HandlerMianShi2" />

        <activity android:name="dispatching_events.DispatchEventMianShi" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>